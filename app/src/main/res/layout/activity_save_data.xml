<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:focusable="true"
    android:focusableInTouchMode="true"
    android:background="@android:color/background_light"
    android:orientation="vertical">
    <include layout="@layout/header_expense"/>

    <TextView
        android:id="@+id/text_savestatus"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif"
        android:paddingLeft="10dp"
        android:text="Sin estado de conexión"
        android:textSize="20dp"
        android:layout_marginTop="150dp"
        />

    <EditText
        android:id="@+id/expense_comment"
        android:layout_width="match_parent"
        android:layout_height="250dp"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/text_savestatus"
        android:layout_marginTop="8dp"
        android:singleLine="false"
        android:hint="Comentario"
        android:gravity="left|top"
        android:paddingLeft="10dp"
        />

    <Button
        android:id="@+id/button_save_expense"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@+id/expense_comment"
        android:text="Guardar información" />
</RelativeLayout>